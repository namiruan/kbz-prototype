# 완료 화면 장식

`quote-doc.png` — 견적서를 형상화한 3D 글래스 아이콘.
완료 화면(`.is-done`) 우측 하단에 둥둥 떠 있는 장식으로 쓴다.

- 쓰는 곳: `quote.html`, `face-attendance-quote.html` 의 `body.quote-page.is-done::after`
- 배경 이미지라 파일이 없으면 아무것도 그리지 않는다. 화면이 깨지지 않는다.
- 권장: 정사각 PNG, 배경 투명, 긴 변 600px 이상.
